const Platform = () => {
  return (
    <div className="page">
      <h1>Platform Architecture</h1>
      <p>
        QuantumShield integrates Hybrid SVM + QSVM models,
        real-time risk scoring, and adaptive authentication.
      </p>
    </div>
  );
};

export default Platform;
